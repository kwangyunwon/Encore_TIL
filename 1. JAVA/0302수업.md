for = 반복횟수를 알때
while = 몇번 반복할지 모를 때

1. 반복문에서 break키워드는 조건문(if)과 함께 쓰인다. O
2. if문에서 break키워드를 사용하면 if문을 빠져나간다. X -> 반복문을 빠져나간다.

Array = 데이터 타입이 같은 여러개의 값들을 하나의 변수를 통해서 한방에 처리하는 것
1. 선언				int [] arr;
2. 생성(반드시 배열 size 명시)	arr = new int[3];	
3. 초기화
